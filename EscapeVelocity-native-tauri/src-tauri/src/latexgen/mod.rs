//! Structured model → `memoir` LaTeX generation — the deterministic core
//! contract `(document.json, settings.json) -> main.tex`, plus the node-id ↔
//! `.tex` line-range source map that lets SyncTeX jumps resolve to structured
//! nodes. Pure and golden-tested. Implemented in M2.
