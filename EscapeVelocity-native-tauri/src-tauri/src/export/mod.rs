//! Export & print-correctness: produce a print-ready PDF (embedded/subset
//! fonts, optional bleed, even page count, no crop marks) and run the KDP
//! preflight validator before export. Also exports the `.tex` + assets bundle.
//! Implemented in M6.
